# Lessons learned

- README feature lists in this repo were marketing, not implementation. Trace execution.
- A second localStorage key is not a backup. It is a second, stale product.
- Demo seed data plus no persistence is worse than an empty state.
- Template nouns (candidate, resume, fit score) must lose to repository evidence.
- Do not add CRDTs to fix “refresh loses my todo.”
